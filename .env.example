# Workspace/env configuration
AIDE_WORKSPACE=/Users/you/aide-workspace
AIDE_ENGINE=/Users/you/Github/Aide

# Telegram formatting: plain (default) or markdown_v2
AIDE_TELEGRAM_PARSE_MODE=plain
# Markdown escaping: none (default) or aggressive
AIDE_TELEGRAM_ESCAPE=none
# Progress status updates while running
AIDE_TELEGRAM_PROGRESS=1

# Claude Code permissions (skip interactive prompts)
AIDE_CLAUDE_SKIP_PERMISSIONS=1

# Slack: auto-reply in threads without needing @mention (default: 0)
# When enabled, bot responds to all messages in threads where it already has a session.
# Requires additional Slack event subscriptions: message.channels (and message.groups for private channels)
# and OAuth scopes: channels:history (and groups:history for private channels).
AIDE_SLACK_AUTO_THREAD=0

# Max attachment size in MB (Telegram uploads)
AIDE_TELEGRAM_MAX_FILE_MB=10

# Scheduler concurrency
AIDE_SCHEDULER_WORKERS=2

# Telegram
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
ALLOWED_USERS=123456789,987654321
AIDE_DEFAULT_CHAT_ID=123456789
